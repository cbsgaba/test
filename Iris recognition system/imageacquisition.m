function [img] = imageacquisition()

%Note: comments = NOT NEEDED FOR NOW. USING DUMMIES
%clear rpi
%clear cam
%rpi = raspi();
%cam = cameraboard(rpi, 'Resolution', '800x600');
    

%for i = 1:100
%    img = snapshot(cam);
%    image(img);
%    drawnow;
%end

%figure;
%imggray = rgb2gray(img); %function that converts RGB image to Gray image
%subplot(2,2,1),imshow(img)
%title('RGB Image')
%subplot(2,2,2),imshow(imggray)
%title('Gray Image')

end
